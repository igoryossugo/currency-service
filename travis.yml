dist: xenial
language: python
python:
  - "3.9"

cache:
  directories:
    - $HOME/.cache/pip

install:
  - pip install -r requirements-dev.txt

script:
  - make check coverage

after_success:
  - pip install codecov pyOpenSSL
  - codecov
